#!/bin/sh

RPI_ROOT=$(realpath $(dirname $0)/..)

cp $RPI_ROOT/bin/emu-install $RPI_ROOT/root/tmp/emu-install
$RPI_ROOT/bin/rpi-chroot tmp/emu-install

killall /usr/bin/qemu-arm-static
sleep 2

rm -fr $RPI_ROOT/root/tmp/*
